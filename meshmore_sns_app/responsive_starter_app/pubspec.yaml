name: meshmore_sns_app
description: "Meshmore SNS — a Flutter client for the Meshcore LoRa mesh network."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.165+1

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # --- Core / UI ---
  cupertino_icons: ^1.0.8
  responsive_toolkit: ^0.0.6
  flutter_native_splash: ^2.4.3

  # --- App metadata ---
  package_info_plus: ^8.1.0
  device_info_plus: ^11.2.0

  # --- State management ---
  provider: ^6.1.2

  # --- Navigation (R11: swipe + long-press menu) ---
  go_router: ^14.6.2

  # --- Localization (R4: EN + JA) ---
  intl: ^0.20.2

  # --- Meshcore companion protocol (pure-Dart, in-repo package) ---
  meshcore:
    path: ../packages/meshcore

  # --- Bluetooth to Meshcore device (R3) ---
  flutter_blue_plus: ^1.34.5
  permission_handler: ^11.3.1

  # --- Phone GPS one-shot for R22 / U13 (device-first, phone-fallback) ---
  geolocator: ^13.0.1

  # --- Text-to-Speech (R5, off by default) ---
  flutter_tts: ^4.2.2

  # --- Per-theme audio cue assets (R12) ---
  audioplayers: ^6.1.0

  # --- Settings persistence (R7) ---
  shared_preferences: ^2.3.3

  # --- Android background keep-alive (R17 / U8): foreground service
  #     so the BLE link + SYNC-drain survive Doze. Android-only;
  #     isolated behind BackgroundKeepalive (no-op elsewhere). ---
  flutter_foreground_task: ^9.2.2

  # --- R32 dice-roll PSK derivation — accelerometer entropy via
  #     sensors_plus. Used only in the channels Edit dialog's Shake
  #     mode; no background sampling. crypto for SHA-256 mix. ---
  sensors_plus: ^6.0.1
  crypto: ^3.0.6

  # --- R6 follow-on: QR-share for channel PSK handoff. Generation
  #     only in this pass; scanning side is captured separately.
  qr_flutter: ^4.1.0

  # --- Magnetometer-derived compass heading for the radial /grid
  #     "facing arrow" overlay. Wraps the platform's native sensor
  #     fusion (Android ROTATION_VECTOR, iOS CLLocationManager
  #     heading). Subscribed only while the radial view is mounted.
  flutter_compass: ^0.8.1

  # --- External-URL launcher for "Show on map" (NodeDetailSheet
  #     → Apple Maps / Google Maps), QR-share text fallback, etc.
  url_launcher: ^6.3.1

  # --- R25 Stage 3 — OSM raster tile renderer for the equal-grid
  #     view background. Used in tile-layer-only mode (no panning /
  #     zooming from the user — the equal-grid view is fixed,
  #     centred on self). flutter_map's default NetworkTileProvider
  #     handles HTTP fetch + in-memory cache; persistent on-disk
  #     caching is deferred to a later iteration.
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # --- R53 Docs reader — pure-Dart Markdown → AST parser. We render
  #     the AST to widgets ourselves (futuristic monospace styling),
  #     so no rendering dependency is pulled in. Docs (firmware README,
  #     companion-protocol wiki, this app) are baked in as offline
  #     snapshots and refreshed opportunistically over HTTP. Fetch uses
  #     dart:io behind an injectable seam; cache is SharedPreferences.
  markdown: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.3

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # R4 / U5 — code-gen the AppLocalizations class from
  # lib/l10n/*.arb at build time. See l10n.yaml for the
  # template + output paths.
  generate: true

  
  # To add assets to your application, add an assets section, like this:
  assets:
      - assets/images/icon-192.png
      # R12 per-theme audio cue packs — 7 WAVs per theme, synth'd
      # offline by brand/_audio_pack.py.
      - assets/audio/seele/
      - assets/audio/nerv/
      - assets/audio/aghud/
      - assets/audio/hyperlocal/
      - assets/audio/drpop/
      - assets/audio/recon/
      # R27 globe view — Natural Earth 110m land polygons (public
      # domain). ~97 KB after coord-trim to 4 decimals. Loaded once
      # at GlobeView startup.
      - assets/data/world-110m.geojson
      # R25 Stage 2 — packed GeoNames cities15000 dataset (~33 k
      # cities, ~810 KB). CC-BY 4.0 (attribute geonames.org in About).
      # Built offline via tool/pack_cities.dart; re-run when the
      # GeoNames source refreshes.
      - assets/data/cities15000.bin
      # Per-device power reference for battery-life estimation
      # (capacity, OCV curve, typical current draw). Offline-first:
      # baked into the binary like the region presets; bump the app
      # to refresh. Matched against DeviceInfo.manufacturer.
      - assets/data/device_power_specs.json
      # R53 Docs reader — offline snapshots of the MeshCore firmware
      # README + companion-radio-protocol wiki, plus this app's own
      # self-documentation. Refreshed opportunistically from GitHub.
      - assets/docs/firmware.md
      - assets/docs/protocol.md
      - assets/docs/app.md

  # R55 skin system — the UX brief's OSS type spine. Saira is the
  # Eurostile-class display face (shared across concepts); JetBrains
  # Mono is the NERV-concept telemetry/numeric face (slashed-zero via
  # FontFeature). Both are *variable* fonts — fontWeight maps to the
  # wght axis at runtime. OFL; attributed in About. ~670 KB total.
  fonts:
    - family: Saira
      fonts:
        - asset: assets/fonts/Saira.ttf
    - family: JetBrains Mono
      fonts:
        - asset: assets/fonts/JetBrainsMono.ttf
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

flutter_native_splash:
  # This package generates native code to customize Flutter's default white native splash screen
  # with background color and splash image.
  # Customize the parameters below, and run the following command in the terminal:
  # dart run flutter_native_splash:create
  # To restore Flutter's default white splash screen, run the following command in the terminal:
  # dart run flutter_native_splash:remove

  # IMPORTANT NOTE: These parameter do not affect the configuration of Android 12 and later, which
  # handle splash screens differently that prior versions of Android.  Android 12 and later must be
  # configured specifically in the android_12 section below.

  # color or background_image is the only required parameter.  Use color to set the background
  # of your splash screen to a solid color.  Use background_image to set the background of your
  # splash screen to a png image.  This is useful for gradients. The image will be stretch to the
  # size of the app. Only one parameter can be used, color and background_image cannot both be set.
  color: "#42a5f5"
  #background_image: "assets/background.png"

  # Optional parameters are listed below.  To enable a parameter, uncomment the line by removing
  # the leading # character.

  # The image parameter allows you to specify an image used in the splash screen.  It must be a
  # png file and should be sized for 4x pixel density.
  #image: assets/splash.png

  # The branding property allows you to specify an image used as branding in the splash screen.
  # It must be a png file. It is supported for Android, iOS and the Web.  For Android 12,
  # see the Android 12 section below.
  #branding: assets/dart.png

  # To position the branding image at the bottom of the screen you can use bottom, bottomRight,
  # and bottomLeft. The default values is bottom if not specified or specified something else.
  #branding_mode: bottom
  
  # Set the branding padding from the bottom of the screen.  The default value is 0
  # branding_bottom_padding: 24

  # The color_dark, background_image_dark, image_dark, branding_dark are parameters that set the background
  # and image when the device is in dark mode. If they are not specified, the app will use the
  # parameters from above. If the image_dark parameter is specified, color_dark or
  # background_image_dark must be specified.  color_dark and background_image_dark cannot both be
  # set.
  #color_dark: "#042a49"
  #background_image_dark: "assets/dark-background.png"
  #image_dark: assets/splash-invert.png
  #branding_dark: assets/dart_dark.png

  # From Android 12 onwards, the splash screen is handled differently than in previous versions.
  # Please visit https://developer.android.com/guide/topics/ui/splash-screen
  # Following are specific parameters for Android 12+.
  android_12:
    # The image parameter sets the splash screen icon image.  If this parameter is not specified,
    # the app's launcher icon will be used instead.
    # Please note that the splash screen will be clipped to a circle on the center of the screen.
    # App icon with an icon background: This should be 960×960 pixels, and fit within a circle
    # 640 pixels in diameter.
    # App icon without an icon background: This should be 1152×1152 pixels, and fit within a circle
    # 768 pixels in diameter.
    #image: assets/android12splash.png

    # Splash screen background color.
    #color: "#42a5f5"

    # App icon background color.
    #icon_background_color: "#111111"

    # The branding property allows you to specify an image used as branding in the splash screen.
    #branding: assets/dart.png

    # The image_dark, color_dark, icon_background_color_dark, and branding_dark set values that
    # apply when the device is in dark mode. If they are not specified, the app will use the
    # parameters from above.
    #image_dark: assets/android12splash-invert.png
    #color_dark: "#042a49"
    #icon_background_color_dark: "#eeeeee"

  # The android, ios and web parameters can be used to disable generating a splash screen on a given
  # platform.
  #android: false
  #ios: false
  #web: false

  # Platform specific images can be specified with the following parameters, which will override
  # the respective parameter.  You may specify all, selected, or none of these parameters:
  #color_android: "#42a5f5"
  #color_dark_android: "#042a49"
  #color_ios: "#42a5f5"
  #color_dark_ios: "#042a49"
  #color_web: "#42a5f5"
  #color_dark_web: "#042a49"
  #image_android: assets/splash-android.png
  #image_dark_android: assets/splash-invert-android.png
  #image_ios: assets/splash-ios.png
  #image_dark_ios: assets/splash-invert-ios.png
  #image_web: assets/splash-web.gif
  #image_dark_web: assets/splash-invert-web.gif
  #background_image_android: "assets/background-android.png"
  #background_image_dark_android: "assets/dark-background-android.png"
  #background_image_ios: "assets/background-ios.png"
  #background_image_dark_ios: "assets/dark-background-ios.png"
  #background_image_web: "assets/background-web.png"
  #background_image_dark_web: "assets/dark-background-web.png"
  #branding_android: assets/brand-android.png
  #branding_bottom_padding_android: 24
  #branding_dark_android: assets/dart_dark-android.png
  #branding_ios: assets/brand-ios.png
  #branding_bottom_padding_ios: 24
  #branding_dark_ios: assets/dart_dark-ios.png
  #branding_web: assets/brand-web.gif
  #branding_dark_web: assets/dart_dark-web.gif

  # The position of the splash image can be set with android_gravity, ios_content_mode, and
  # web_image_mode parameters.  All default to center.
  #
  # android_gravity can be one of the following Android Gravity (see
  # https://developer.android.com/reference/android/view/Gravity): bottom, center,
  # center_horizontal, center_vertical, clip_horizontal, clip_vertical, end, fill, fill_horizontal,
  # fill_vertical, left, right, start, or top.
  #android_gravity: center
  #
  # ios_content_mode can be one of the following iOS UIView.ContentMode (see
  # https://developer.apple.com/documentation/uikit/uiview/contentmode): scaleToFill,
  # scaleAspectFit, scaleAspectFill, center, top, bottom, left, right, topLeft, topRight,
  # bottomLeft, or bottomRight.
  #ios_content_mode: center
  #
  # web_image_mode can be one of the following modes: center, contain, stretch, and cover.
  #web_image_mode: center

  # The screen orientation can be set in Android with the android_screen_orientation parameter.
  # Valid parameters can be found here:
  # https://developer.android.com/guide/topics/manifest/activity-element#screen
  #android_screen_orientation: sensorLandscape

  # To hide the notification bar, use the fullscreen parameter.  Has no effect in web since web
  # has no notification bar.  Defaults to false.
  # NOTE: Unlike Android, iOS will not automatically show the notification bar when the app loads.
  #       To show the notification bar, add the following code to your Flutter app:
  #       WidgetsFlutterBinding.ensureInitialized();
  #       SystemChrome.setEnabledSystemUIMode(SystemUiMode.manual, overlays: [SystemUiOverlay.bottom, SystemUiOverlay.top], );
  #fullscreen: true

  # If you have changed the name(s) of your info.plist file(s), you can specify the filename(s)
  # with the info_plist_files parameter.  Remove only the # characters in the three lines below,
  # do not remove any spaces:
  #info_plist_files:
  #  - 'ios/Runner/Info-Debug.plist'
  #  - 'ios/Runner/Info-Release.plist'

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/ios/AppIcon@3x.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: true
    image_path: "assets/images/ios/AppIcon~ios-marketing.png"
    background_color: "#hexcode"
    theme_color: "#hexcode"
  windows:
    generate: true
    image_path: "assets/images/ios/AppIcon-40@2x.png"
    icon_size: 48 # min:48, max:256, default: 48
  macos:
    generate: true
    image_path: "assets/images/ios/AppIcon-40@2x.png"

